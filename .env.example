# Copy this file to .env and fill in your keys. .env is gitignored.
#
# Required — the script exits early without these:
ANTHROPIC_API_KEY=
AGENTMAIL_API_KEY=

# Optional — improves search quality; the script falls back to DuckDuckGo if unset:
FIRECRAWL_API_KEY=
TAVILY_API_KEY=
